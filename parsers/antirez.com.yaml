placeholders:
  url: http://antirez.com/news/130

steps:
  - get
  - parallel:
      meta:
        - meta
        - eval: "map(merge(__, {author: 'Salvatore Sanfilippo', publisher: 'antirez'}))"
      html:
        - find: pre
        - html
        - replace:
            - /\n/g, </p><p>
