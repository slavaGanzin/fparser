steps:
  # - db:
  #     action: skip
  #     key: processed_pages
  #     id: $url
  - get:
      url: $url
  - parallel:
      url:
        - input: $url
      links:
        - find: a
        - attr: href
        # - url: fullpath
      #   - replace:
      #     - /(.*)/ $url$1
      #     - /#.*/
      #   # - filter:
      #   #   - $url
      #   - forward:
      #       name: parsers/links.yaml
      #       placeholders:
      #         $url: $1
      # baseurl:
      #   - input: $url
      #   - url: host
      # html:
      #   - html
  # - db:
  #     key: pages
  #     id:  $url
