placeholders:
  url: https://detailed.com/50/

steps:
  - get
  - find: .blog-url a
  - attr: href
  - reject:
    - /50/ig
  - replace:
  - eval: map(compose(console.log, replace(/www\.|\/$/g, ''), replace(/https?:\/\//, '- [ ] ')))
