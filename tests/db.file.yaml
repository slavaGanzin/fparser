db:
  provider: file
  
steps:
  - input:
      id: id
      value: value
  - db:
      key: /tmp/test
      id: id
  - expect:
      eql:
        - /tmp/test/id
   
  
  - input:
      id: id
  - db:
      action: get
      id: id
      key: /tmp/test
  - expect:
      eql:
        - id: id
          value: value
  
  - input:
      id: asdasd
  - db:
      action: has
      key: test
  - expect:
      eql:
        - false
  
  - input:
      id: id
  - db:
      action: has
      key: /tmp/test
  - expect:
      eql:
        - true
        
