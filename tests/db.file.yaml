steps:
  - input:
      id: id
      value: value
  - db:
      provider: file
      keyPrefix: /tmp/test
      id: id
  - expect:
      eql:
        - id: id
          value: value
      
      
  - input:
      id: id
  - db:
      provider: file
      action: get
      id: id
      keyPrefix: /tmp/test
  - expect:
      eql:
        - id: id
          value: value
