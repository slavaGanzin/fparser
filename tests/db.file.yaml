steps:
  - input:
      id: id
      value: value
  - db:
      provider: file
      key: /tmp/test
      id: id
      # preprocessor: json
  - expect:
      eql:
        - id: id
          value: value
      
      
  - input:
      id: id
  - db:
      provider: file
      action: get
      id: id
      key: /tmp/test
  - expect:
      eql:
        - id: id
          value: value
