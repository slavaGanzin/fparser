steps:
  # - input:
  #     forwardInput: forward
  # - forward:
  #     name: tests/placeholders.yaml
  #     placeholders:
  #       forwardPlaceholders: 'forwardPlaceholders'
  # - expect:
  #     eql:
  #       - [ value, q, forward, forwardPlaceholders]
  #
  #
  - input:
      - input
      - input2
      
  - forward:
      pre:
        - replace:
          - input, preOne
          - preOne, pre
      
      placeholders:
        url: $1
        
      name: tests/mock.yaml
      
      post:
        - replace:
          - pre, post
          
  - expect:
      eql:
        - [ post ]
        - [ post2 ]
        
        
