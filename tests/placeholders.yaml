placeholders:
  url: url
  forwardInput: ''
  forwardPlaceholders: ''
  
steps:
  - input:
     - value
     - $url
     - $forwardInput
     - $forwardPlaceholders
  - expect:
      eql:
        - value
        - url
        - $forwardInput
        - $forwardPlaceholders
    
